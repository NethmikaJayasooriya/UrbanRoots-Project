import 'package:flutter/material.dart';

class AppColors {
  // 🌿 Background
  static const Color bg = Color(0xFF07160F);

  // 🌿 Card surface (slightly elevated tone)
  static const Color card = Color(0xFF0F241B);

  // 🌿 Accent (Primary Green)
  static const Color accent = Color(0xFF3BFF8F);

  // 🌿 Main Text
  static const Color text = Color(0xFFEAF4EE);

  // 🌿 Secondary / Muted Text
  static const Color muted = Color(0xFF8FA9A0);

  // 🌿 Borders / Dividers
  static const Color border = Color(0xFF1E3A2E);

  // 🌿 Subtle Text (for timestamps, helper text)
  static const Color subText = Color(0xFFA6C0B6);

  // 🌿 Soft action background (buttons inside cards)
  static const Color actionBg = Color(0xFF123A29);

  // 🌿 Optional aliases (for compatibility)
  static const Color stroke = border;
}
