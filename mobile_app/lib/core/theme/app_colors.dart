import 'package:flutter/material.dart';

class AppColors {
  // Main background for the app
  static const Color backgroundColor = Color(0xFF07160F);
  
  // Surface color for cards and containers
  static const Color surfaceColor = Color(0xFF16201B);
  
  // Brand neon green for active states and highlights
  static const Color primaryGreen = Color(0xFF00E676);
  
  // Status Colors for plant health
  static const Color healthy = Color(0xFF00E676);
  static const Color thirsty = Colors.orangeAccent;
  static const Color danger = Colors.redAccent;

  // Text hierarchy colors
  static const Color textMain = Colors.white;
  static const Color textDim = Colors.white38;
}