<?xml version="1.0" encoding="utf-8"?>
<!--
  Allow cleartext HTTP for local dev backend IPs.
  10.0.2.2 = Android emulator loopback to host
  192.168.x.x / 10.x.x.x = typical LAN for real devices
  Remove or restrict this before production release.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <!-- LAN ranges for real device testing -->
        <domain includeSubdomains="true">192.168.0.0</domain>
        <domain includeSubdomains="true">10.0.0.0</domain>
    </domain-config>
</network-security-config>
