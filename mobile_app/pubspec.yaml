name: mobile_app # Changed to match your internal imports!
description: "UrbanRoots Smart Garden Application"

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.10.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # UI & Styling
  cupertino_icons: ^1.0.8
  google_fonts: ^8.0.1
  font_awesome_flutter: ^10.6.0
  lucide_icons: 0.257.0
  
  # Networking & Core
  http: ^1.6.0
  js: ^0.6.7
  
  # Firebase & Authentication
  firebase_core: ^4.4.0
  firebase_auth: ^6.1.4
  google_sign_in: ^6.2.2
  image_picker: ^1.2.1
  shared_preferences: ^2.3.0
  firebase_database: ^12.1.4
  cloud_firestore: ^6.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/fonts/
   
  fonts:
    - family: FiraSans
      fonts:
        - asset: fonts/FiraSans-Regular.ttf